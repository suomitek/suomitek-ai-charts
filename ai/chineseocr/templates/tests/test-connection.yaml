apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chineseocr.fullname" . }}-test-connection"
  labels:
{{ include "chineseocr.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "chineseocr.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
